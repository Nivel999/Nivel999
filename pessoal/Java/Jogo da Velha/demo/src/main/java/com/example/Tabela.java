package com.example;
public class Tabela {
   
    public String Tabela(char[][] tab, int linha, int coluna) {
        return "|" + tab[linha][coluna] + "|" + tab[linha][coluna] + "|" + tab[linha][coluna] + "|";
    }
}
